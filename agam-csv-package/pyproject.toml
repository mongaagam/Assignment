[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
[project]
name = "agam-csv-package"
version = "0.1.0"
description = "A simple CSV statistics package"
readme = "README.md"
requires-python = ">=3.8"
[tool.setuptools.packages.find]
include = ["agam*"]
